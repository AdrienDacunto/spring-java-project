un contact peut émettre plusieurs message : OK

un contact peut aussi recevoir plusieurs messages : Ok

un message peut être envoyé à plusieurs destinataires: Ok 

un message peut être le successeur d’un (unique) autre (messages 2, 3, 4) ou non (message 1): Ok

un message peut avoir plusieurs successeurs (message 1) mais toujours un seul prédécesseur: Ok

DbDiagram:

table Utilisateur  
{
  idUtilisateur int [pk, increment] // auto-increment
  login varchar
  password varchar //Don't forget to hash it
  date_de_naissance date
  first_name varchar
  name varchar
  sexe varchar 
}

ref: Utilisateur.idUtilisateur < Message.idEmmeteur
ref: Utilisateur.idUtilisateur < Envoi.idDestinataire
ref: Message.idMessage < Envoi.idMessage

table Message {
  idMessage int [pk, increment]
  contenu text 
  dateEnvoi datetime
  status varchar
  idEmmeteur int
  idPredecesseur int
}

table Envoi{
  idDestinataire int [pk]
  idMessage int [pk]
}